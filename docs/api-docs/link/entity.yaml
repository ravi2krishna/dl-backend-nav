/link/{id}:
  get:
    tags:
      - Link
    description:
      Search by entity.
    parameters:
      - name: id
        in: path
        required: true
        type: string
    responses:
        200:
            description: "status:1 is success; status: 0 is failure;"
